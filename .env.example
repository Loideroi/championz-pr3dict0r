# ₵h@mpi0nz Pr3dict0r — environment variables
# Copy to .env.local (gitignored). NEVER commit real values.

# --- Chain ---
NEXT_PUBLIC_CHAIN_ID=88882                 # 88882 Spicy staging · 88888 mainnet
NEXT_PUBLIC_RPC_URL=https://spicy-rpc.chiliz.com
CHILIZ_RPC_URL=https://spicy-rpc.chiliz.com # server-side RPC

# --- Wallet connection (Reown/WalletConnect) ---
NEXT_PUBLIC_WALLETCONNECT_PROJECT_ID=       # cloud.reown.com

# --- Contracts ---
NEXT_PUBLIC_PREDICTOR_ADDRESS=
NEXT_PUBLIC_TROPHY_ADDRESS=                 # ChampionzTrophy ERC-721 (slice 11)

# --- Supabase (existing free project; clp_ tables) ---
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=
SUPABASE_SERVICE_ROLE_KEY=                  # server only

# --- Telegram bot (slice 06/10) ---
TELEGRAM_BOT_TOKEN=
TELEGRAM_OPS_CHAT_ID=                       # private ops channel
TELEGRAM_CHANNEL_ID=                        # public channel

# --- Relayer / oracle (GitHub Actions secrets, mirrored here for local runs) ---
ORACLE_PRIVATE_KEY=                         # low-value key, gas only — NEVER the owner key
ADMIN_SYNC_SECRET=                          # gate for admin/manual endpoints

# --- Crest fallback source ---
FOOTBALL_DATA_TOKEN=                        # api.football-data.org free tier
