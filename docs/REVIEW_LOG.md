# Review Log

Per-PR record required by the multi-agent code review contract (Loideroi LLM Wiki, `agent/contracts/multi-agent-code-review.md`): tier, reviewers with exact model IDs, findings by severity, dispositions, disputes.

## 2026-09-13 — PR #108 (hash-pinned Python requirements for the slither gate)

**Scope.** R1 reviewed `b88ca41` (implementation, +1814/−6 across 4 files) and re-checked `d8ec244` (wording fix + this entry: +1843/−6 across 5 files, of which 1790 lines are the generated lock and 28 this entry — implementation delta excluding the log ≈ 25 hand-written lines; a trivial PR-number commit follows). Closes the follow-up named in PR #105: the `contracts` job installed `slither-analyzer==0.11.4` with its 48 transitive packages floating. New `contracts/requirements-slither.in` (`slither-analyzer==0.11.4`) and `contracts/requirements-slither.txt` — `pip-compile --generate-hashes --strip-extras` output (pip-tools 7.6.1 in a throwaway venv, CPython 3.11.14, macOS arm64): 49 packages, exact versions plus the sha256 of every file PyPI publishes per release (1630 hashes). CI step "Install slither (hash-pinned requirements)": `python -m pip install --quiet --require-hashes --only-binary=:all: -r contracts/requirements-slither.txt`; `python-version: "3.11"`, step order and the `npm run test:gate && npm run slither` invocation unchanged. `docs/REVIEW_TIERS.md`: both requirements files added to the Tier 3 guardrail-config floor row; slither baseline row updated; Named Follow-Up "Slither Python pins" struck as Done with the local install path, the refresh procedure, the red-capable evidence, and the residual boundary (interpreter patch, pip, action tag not hashed). **No Solidity, no npm dependency, no gate-logic change; no Python tooling added to the repo.**

**Tier.** 3 — workflow, guardrail config (`contracts/requirements-slither.*`), `docs/REVIEW_TIERS.md`. Plan reviewed before code.

**Roles and models.** Author: Claude Code, `claude-opus-5[1m]` (subagent builder, own worktree `chore/slither-hash-pins`). Plan reviewer + R1 (cross-vendor): Codex CLI `gpt-5.6-sol`, `model_reasoning_effort=high`, read-only sandbox (could not create venvs or run pip; verified by reading, by machine-parsing the YAML and auditing all 1790 lock lines, and by running the judges). R2 (fresh-context): **pending (orchestrator)**.

**Plan review (R1).** FAIL — 3 Major (sdist hashes in the lock but CI allowed source builds, whose isolated build environments fetch unpinned build dependencies → `--only-binary=:all:` in CI, the documented local install, and the wheel probe; macOS/Linux portability asserted from a one-platform compile → evidenced by a marker audit over the locked set plus the Linux wheel probe, with CI on Linux named as the final proof; flipping one of many hashes can leave a matching hash and a warm cache can satisfy the package → negative tests redone in fresh venvs with `--no-cache-dir`, one package's whole hash list replaced by a bogus hash and another package's hashes removed), 2 Minor (setup-python action tag / 3.11.x patch / pip float — recorded as the residual boundary, not described as hermetic; refresh command must pin `pip-tools==7.6.1` and record the real command in the header via `CUSTOM_COMPILE_COMMAND`). All taken.

**Verdicts.** R1 at `b88ca41`: **pass-with-minors** — 1 Minor ("every locked package ships a manylinux wheel" is inaccurate: `slither-analyzer` and other pure-Python packages ship `py3-none-any` wheels — reworded to "a wheel usable on manylinux x86-64, manylinux-tagged or platform-independent" in the CI comment and REVIEW_TIERS). All three plan Majors and both Minors confirmed resolved on this head. R1 round 2 at `d8ec244`: **pass-with-minors** — 1 Minor (log diff accounting; fixed in `4906801`). R1 final at `c6aa6a0` (PR number substituted): **PASS**, 0/0/0/0. R2: pending (orchestrator). Tally so far: 0 Blocker / 0 Major / 2 Minor (both resolved) / 0 Nit. No adjudication.

**Author verification.** Fresh `python3.11 -m venv` → `pip install --require-hashes --only-binary=:all: -r contracts/requirements-slither.txt` exit 0; `slither --version` → 0.11.4; `pip check` clean; 49 locked packages installed. `pip download --only-binary=:all: --platform manylinux2014_x86_64 --platform manylinux_2_17_x86_64 --platform manylinux_2_28_x86_64 --python-version 3.11 --implementation cp --abi cp311 --abi abi3 --abi none --require-hashes --no-deps` → 49 wheels, exit 0. Marker audit (`packaging.markers` over every `Requires-Dist` of the 49 dists): 211 conditional requirements, zero differ between linux-x86_64 and darwin-arm64 at Python 3.11, zero evaluate true while unlocked; the only unconditional extra (`web3 → eth-hash[pycryptodome]`) is satisfied. Red-capable in fresh venvs with `--no-cache-dir`: (A) `slither-analyzer`'s hash list replaced by one all-zero sha256 → exit 1, `THESE PACKAGES DO NOT MATCH THE HASHES FROM THE REQUIREMENTS FILE`, names `slither_analyzer-0.11.4-py3-none-any.whl`; (B) `crytic-compile`'s hash lines removed → exit 1, `Hashes are required in --require-hashes mode`. Gate: `npm ci --ignore-scripts` in `contracts/`, `npm run test:gate` 15/15, `npm run slither` with the hashed venv on `PATH` → `check-slither: OK — 8 High/Medium finding(s), all 8 triaged in slither-triage.json with matching metadata; 35 lower-severity finding(s) informational` (unchanged from main); no report or log left in cwd. Judges: `check-agents-md.mjs` (self-test 47 + AGENTS.md), `lint-review-log.mjs` (self-test 43 + this file) pass; `git diff --check` clean; YAML parsed, folded scalar yields exactly the one-line pip command.

**Risk brief (R1).** *What changed:* slither's floating transitive dependencies are replaced by a 49-package, hash-pinned, binary-only lock installed by the `contracts` required check; the finding gate itself is untouched. *What could break:* platform compatibility — Linux CI must find a compatible wheel for every pin (evidence: the lock structure, the manylinux probe, the marker audit; the PR's own CI run is the final proof, and a mismatch fails the install rather than passing anything unhashed). *What was checked:* every lock line mechanically (49 pins, 1630 valid hashes, no unhashed pins, duplicates, URLs, index overrides or markers), the YAML, the unchanged gate scripts/config/triage, leftover references to the old install command (only the historical PR #105 log entry), tier-floor and follow-up wording, the judges. *Residual risk:* the Python 3.11 patch, pip, and the setup-python action tag still float (recorded boundary); no meta-test protects the two enforcement flags — deleting them would stay green and relies on Tier 3 review of the workflow (accepted; same standing as every other workflow step here).

**Checked.** R1: `git diff origin/main...HEAD` in full for the hand-written files; all 1790 lock lines audited mechanically (49 unique exact pins, 1630 64-hex sha256 hashes, no unhashed pin, duplicate name, malformed hash, URL/VCS source, index override or retained marker); header, tail and the `crytic-compile` (2 hashes), `cytoolz` (173), `slither-analyzer` (2) stanzas; YAML machine-parsed; `git diff --exit-code` on the gate scripts, config, triage and `contracts/package.json` (unchanged); grep for the former install command; the Tier 3 floor row, pinned `pip-tools==7.6.1` refresh command, `CUSTOM_COMPILE_COMMAND`, portability evidence and residual boundary in REVIEW_TIERS; ran `git diff --check`, `node scripts/check-agents-md.mjs AGENTS.md`, `node scripts/lint-review-log.mjs --self-test` (43) and the target lint (7 entries); non-generated delta below the 500-line cap. Author: the list under Author verification.

**verification-gap.** A corrupted, missing, or substituted artifact fails the CI install mechanically while both flags stand (proven red-capable in fresh venvs). Not mechanically protected: removal of `--require-hashes` / `--only-binary=:all:` from the workflow, or a Python minor bump without regenerating the lock — both are Tier 3 workflow edits caught by review, and the latter also fails loudly at install time if any marker-conditional package differs.

**named-set.** `--only-binary=:all:` covers every package, not a list; the lock has exactly the 49 packages the resolver produced with no per-package exceptions; the marker audit evaluated the full `Requires-Dist` set of all 49 dists (211 conditional entries), not a sample; the tier floor names both requirements files.

**Missing.** Looked for and did not find: a sdist-only package (every pin has ≥ 2 hashes and a wheel usable on manylinux x86-64); a requirement needed on Linux but absent from the lock; an environment marker, URL, VCS source or index override in the lock; any other `pip install slither-analyzer` instruction in active docs, templates or `AGENTS.md`; any change to `check-slither.mjs`, `slither.config.json`, `slither-triage.json`, `contracts/package.json`, or the gate's step order; an existing requirements-file convention in the repo (none — first Python requirements file); a pip cache in setup-python (no `cache: pip`, so no stale-cache key to fix).

**Dismissed.** None. Not done by decision, recorded in REVIEW_TIERS: pinning the Python patch or the action by SHA (workflow-wide decision); a `pip` Dependabot ecosystem for the new file (new automation surface — owner option).

**Human gate.** Owner script: `contracts` check green on the PR head with "Install slither (hash-pinned requirements)" visible and the slither step's log ending in `check-slither: OK — 8 High/Medium finding(s), all 8 triaged … with matching metadata`; `main` protection unchanged; R2 verdict logged; decide on the Dependabot `pip` option; merge. **Pending R2 and owner go/no-go.**

## 2026-09-13 — PR #105 (slither in the contracts required check)

**Scope.** Head `af3e96f` (six commits on `f720e68`; +403/−18 across 11 files). `contracts/scripts/check-slither.mjs` (+ 15 node:test cases in `check-slither.test.mjs`) is the gate: with `--run` it compiles (`hardhat compile --force`), runs slither 0.11.4 with `--hardhat-ignore-compile` writing JSON to a fresh mkdtemp path, and fails on any new High/Medium finding, any triage entry whose id or re-derived metadata (check, impact, confidence, function, `file#line`, expression) no longer matches, a malformed entry, a Low-severity entry, zero findings, a config without `show_ignored_findings: true`, or a compile/slither failure (log tail printed). `slither-triage.json`: the 8 accepted findings (1 High `arbitrary-send-eth` to the owner-set `feeRecipient`; 5 Medium `incorrect-equality` in `_score` with exact expressions; 2 Medium `uninitialized-local` accumulators) cross-referenced to SECURITY_FINDINGS.md §Slither. `slither.config.json` (`filter_paths`, `fail_on: medium`, `show_ignored_findings`). CI `contracts` job: Python 3.11, `python -m pip install slither-analyzer==0.11.4`, `npm run test:gate && npm run slither`; the silent-skip `if [ -f package.json ]` guard removed. Docs: REVIEW_TIERS floor rows + baseline row + follow-up closed; PR template; AGENTS; SECURITY_FINDINGS note. **No Solidity source changed.**

**Tier.** 3 — workflow, `contracts/**`, `AGENTS.md`, `docs/REVIEW_TIERS.md`. Plan reviewed before code.

**Roles and models.** Author: Claude Code, `claude-opus-5[1m]`. Plan reviewer + R1 (cross-vendor): Codex CLI 0.144.1 `gpt-5.6-sol`, read-only sandbox (could run slither/hardhat only partially — temp-dir writes denied; verified by reading and by ESLint/JSON probes). R2 (fresh-context): Claude subagent `claude-opus-5[1m]`, own worktrees, ran everything.

**Plan review (R1).** FAIL — 1 High (`fail_medium` is not a slither config key → `fail_on`), 3 Medium (fail-closed claim overstated; five `incorrect-equality` entries indistinguishable; CI should run the package script with `python -m pip`), 4 Low (double compile; Medium threshold justified; docs must name the owning job and not close the follow-up before CI; checklist working directory). All taken.

**Verdicts.** R1 at `a027de6`: **FAIL** — 2 Medium (metadata checked for presence not correctness; no tests), 1 Low (diagnostics discarded). R2 at `a027de6`: **FAIL** — 1 **Major** (an inline `// slither-disable-next-line` comment hides a finding before the report is written: a new ETH send to `msg.sender` passed the gate — proved), 6 Minor (annotations not cross-checked, two `function` values wrong; id-sensitivity overstated for variable-level findings; empty result set passed; `> /dev/null` hid causes; tests untracked; pip transitive deps unpinned), 3 Nit. Fixes `52da998`/`cb64110`/`2e94bec`: `show_ignored_findings: true` + gate refusal without it; metadata re-derived and matched (caught the two wrong `function` values mechanically); zero-findings guard; log capture with tail; tests committed and run in CI; compile once with `--force` then `--hardhat-ignore-compile` (also removes slither's `hardhat clean --global`, which wiped the shared compiler cache during R2's concurrent run). R1 re-check at `2e94bec`: three findings resolved, **new Medium** — if the leading `rm -f` in the npm shell chain failed, the checker consumed a stale report and passed (observed in the read-only sandbox). R2 at `2e94bec`: **PASS** (bypass probe, range-form probe, stale-artifact probes A/B/C). Fix `b52d6f3`: the script owns the run — fresh mkdtemp report path, no shell chain, three regression tests (15 total). `af3e96f`: unused export dropped. **Final at `af3e96f`: R1 PASS, R2 PASS** (R2 attacked the stale-report path five ways incl. an undeletable decoy and a `SLITHER_REPORT` override; all exit 1). Tally: 0 Blocker / 1 Major (R2, resolved) / 4 Medium-equivalent (R1, resolved) / 7 Minor + 1 Low (resolved) / 4 Nit (1 open: temp dir not cleaned on SIGINT). No adjudication; the reviewers found disjoint holes (R1: metadata correctness, tests, stale report; R2: inline-disable bypass, wrong function names, empty report).

**Author verification.** `npx hardhat compile` / `test` (55 passing) / `npm run test:gate` (15/15) / `npm run slither` green; break-the-judge: new ETH send (with and without `slither-disable-next-line`), one-line shift (6 stale + 6 new), entry without SECURITY_FINDINGS reference, metadata mismatch, config without the flag, syntax error, compile-failure-then-revert sequence, stale HIGH report in cwd — each fails as intended; clean tree passes with no report left in cwd.

**Risk brief (R1, final).** *What changed:* the gate owns the analysis run — force-compiles current sources, runs slither on those artifacts, writes to a fresh temp report and validates only that; rejects new or stale High/Medium findings, mismatched triage metadata, Low-severity entries, disabled `show_ignored_findings`, empty analysis, compile failure and missing output; 15 tests run before it in CI. No Solidity or runtime behaviour changed. *What could break:* slither/Hardhat artifact-format compatibility on upgrades; temp-directory availability; CI time and toolchain availability. *What was checked:* every delta, gate logic and probes, triage entries against source lines and SECURITY_FINDINGS, CI ordering, docs, size. *Residual risk:* top-level-only Python pin (hash-pinned requirements are a named follow-up); the two `_score` equality lines are accepted by specification, not proven safe by a tool.

**Checked.** R2 (own worktrees, three rounds, `contracts/`): `npm ci` (0), `npx hardhat compile` (0), `npx hardhat test` (0, 55), `npm run test:gate` (0, 12 then 15), `npm run slither` (0, "8 High/Medium … all 8 triaged … matching metadata; 35 informational", no report left in cwd, no temp dirs left); the eight triage entries against the Solidity lines (`#273`, `#695/#698/#710/#711/#713`, `#628`, `#668`) and SECURITY_FINDINGS.md (C-1b at L22, informational dispositions at L109-110); `filter_paths` effect measured (87 → 43: only a Low and an Informational dropped); `--json` written even on non-zero exit; `slither-report.json` gitignored; CI step order and the unchanged `contracts` job id; script read in full (only `CONFIG` and the mkdtemp path reach `sh -c`); judges: plain ETH send, ETH send + `slither-disable-next-line`, `-start/-end` range, one-line shift (6 stale + 6 new), High entry deleted, duplicate id, missing reason, syntax error (log tail), config without the flag, stale-artifact probes A/B/C, five stale-report attacks incl. an undeletable decoy and a `SLITHER_REPORT` override. R1 (read-only): every delta diff, gate logic, `npm run lint`/`arch` (0), ESLint/JSON probes, `git diff --check`; hardhat/vitest/slither runs blocked by the sandbox's temp-dir denial — which is how it observed the stale-report path.

**verification-gap.** The gate's own 15 tests cover new/stale/mismatched/malformed/Low entries, missing or failed reports, config refusal, zero findings, and the three stale-report paths; a regression in any of those fails `npm run test:gate` before the gate runs. Not covered by a test: slither itself changing its JSON schema or `show_ignored_findings` semantics on an upgrade — the top-level pin (0.11.4) and the dated triage make that a visible, reviewed event rather than a silent pass.

**named-set.** `GATED = {High, Medium}` is the complete blocking set and matches slither's impact vocabulary in 0.11.4 (`Low`, `Informational`, `Optimization` reported only); the triage file holds exactly the eight High/Medium findings and the gate rejects an entry for any other impact; `DERIVED` names every machine-derivable field and all are matched.

**Missing.** Looked for and did not find: a Solidity source change; a way for an inline `slither-disable` comment, an auto-loaded `slither.db.json`, a stale report, stale artifacts, or an empty report to pass; a High/Medium finding dropped by `filter_paths`; a weakened threshold or a removed step; a dependency or lockfile change (slither is a pinned pip tool, not an npm dependency); an existing slither gate or helper to reuse (Fanbet's lives in another repo).

**Dismissed.** None; no disputes, no adjudication. Open by agreement: one Nit (temp dir not removed on SIGINT); follow-ups recorded in REVIEW_TIERS (hash-pinned Python requirements; relayer dependency-cruiser/knip).

**Human gate.** Owner script: `contracts` check green with "Static analysis (slither …)" visible and its log ending in `check-slither: OK — 8 High/Medium finding(s), all 8 triaged … with matching metadata`; `main` protection unchanged; merge. **Pending owner go/no-go.**
## 2026-09-12 — PR #100 (relayer under the root ESLint budgets)

**Scope.** Head `f22ad2e` (five commits, rebased onto `f720e68` after PR #98 merged mid-review; +61/−21 across 11 files). `eslint.config.mjs`: `relayer/**` un-ignored, `relayer/dist/**` ignored, `react-hooks/*` off for `relayer/**` only (Node code; rules-of-hooks keys on `use*` naming), `max-lines` off for exactly `relayer/vendor/uefa-api-types.ts` (vendored third-party types, PRD §7.1). Five dated complexity exceptions in `relayer/src` (toMatchResult 37, renderInsight 20, relayOnce 20, diffOnchain 17, runsFromMatches 17; expire 2026-10-31). Three `no-unused-vars` fixed in code (unused type import; two unused stub parameters on `FootballDataSource` — TS permits fewer implementation params; only consumer is typed as the interface). REVIEW_TIERS baselines (10 exceptions, app + relayer), relayer follow-up closed, relayer dependency-cruiser/knip recorded as a named deferred gap; AGENTS Gates line; knip comment; relayer README; checker test fixture path. Zero new dependencies; no CI YAML change — the root `npm run lint` in the required `app` check now lints 152 files (45 relayer: src, test, scripts/*.mjs, vendor) with root node_modules only (typescript-eslint `recommended`, no type info needed).

**Tier.** 3 — `eslint.config.mjs`, `relayer/**`, `AGENTS.md`, `docs/REVIEW_TIERS.md` floors. Plan reviewed before code.

**Roles and models.** Author: Claude Code, `claude-opus-5[1m]`. Plan reviewer + R1 (cross-vendor): Codex CLI 0.144.1 `gpt-5.6-sol`, read-only. R2 (fresh-context): Claude subagent `claude-opus-5[1m]`, own worktrees.

**Plan review (R1).** FAIL — 2 Major (a directory-wide `relayer/vendor/**` ignore would bypass every rule and the inline-config ban → narrowed to a `max-lines`-only override; the dependency-cruiser/knip deferral was overstated → recorded as a named gap), 4 Minor (document inherited React/Next rules; fix unused-vars in code not `argsIgnorePattern`; list every verification command; defer the `@ts-expect-error` note). All taken.

**Verdicts.** R1 at `55f5bde`: **pass-with-minors** (2 Minor: ESLint comment said five exceptions, now ten; baseline header date) → fixed in `0ab82e6`. R2 at `55f5bde`: **pass-with-minors** — 2 Minor (`react-hooks/rules-of-hooks` fires on `use*`-named Node functions, proved by probe, and the "inert on non-JSX" comment was wrong; vendor exemption glob wider than the doc), 3 Nit (knip comment stale; relayer README; test fixture path), 1 informational (`insights.ts` at 392/400 lines). Fixed in `28fb813`. Re-checks at `28fb813`: **R1 PASS** (hooks override judged legitimate scoping, not weakening — Node-only tree, app React rules untouched), **R2 PASS** (probes: `use*` Node fn passes in relayer and still fails in `app/`; second >400-line vendor file fails; inline config in the exempt file fails; J1/J3/J4 re-run). **Mid-review event:** PR #98 merged to `main` and grew `relayer/src/insights.ts` to 459 counted lines, so the merge-commit CI failed `max-lines`; branch rebased (content byte-identical by patch-id, verified by R2), plus one dated file-level `eslint-disable max-lines` (expires 2026-10-31, split tracked) — 11 exceptions. **Final at `f22ad2e`: R1 PASS, R2 PASS** (R2 proved the directive is counted, fails without/after expiry, exempts only `max-lines`, and self-retires via the unused-directive error once the file shrinks). Tally: 0 Blocker / 0 Major / 4 Minor (all resolved) / 3 Nit (resolved).

**Author verification.** All root gates + build green; relayer typecheck, build, 165 tests green; break-the-judge ×6 in `relayer/src` and vendor (complexity 16, undated disable, inline config in src and in vendor, exception removed, 401-line file) plus the three post-fix probes. Found during verification: a local relayer build produces `relayer/dist` (1,914-line `.d.ts`) that the un-ignored glob linted → ignored next to `.next`/`out`/`build`. R2 additionally verified lint passes with `relayer/node_modules` absent (the CI `app` job's situation), scripts and tests are covered, and `relayer/dist` is gitignored.

**Risk brief (R1, final).** *What changed:* the required root lint gate now includes the value-moving relayer under the same complexity, file-length, expiry and inline-config policies as the app; five existing violations are time-bounded; three unused declarations removed; two React-only rules scoped away from Node code; vendor exemption narrowed to one file. *What could break:* inherited Next/React rules other than hooks could report on future relayer patterns (over-strict, never under-strict); reflective code reading `FootballDataSource.fixtures.length` would see 0 (none exists); React code under `relayer/**` would lose hooks linting until the override is revised. *What was checked:* effective configs per file class, complete relayer file selection, root-only resolution, both lint entry points, exception count, relayer typecheck, call sites and tests of the stubs, diff scope, gate preservation. *Residual risk:* low; R1 could not run Vitest in its sandbox (R2 did, 215 + 165 green).

**Checked.** R2 (own worktrees, three rounds): root `npm ci` → `lint` (0; 10 then 11 exceptions across 152 files, verified with `relayer/node_modules` absent — the CI `app` job's situation), `arch` (0), `deadcode:ci` (0), `check:i18n` (0), `typecheck` (0), `dup` (0, 0.86%), `test` (0, 215), `build` (0); relayer `typecheck`, `build`, `vitest run` (165); base-commit reproduction of the 9 findings with relayer un-ignored; effective ESLint configs per file class; the `FootballDataSource` arity change against its only consumer and tests; `git check-ignore relayer/dist`; patch-id identity of the rebased content; eleven judges (complexity 16, undated disable, inline config in src and in vendor, exception removed, 452-line file, dated-but-unneeded disable, unused const in `relayer/scripts/*.mjs`, complexity 16 in `relayer/test`, junk in `relayer/dist` ignored, expired exception) plus the post-fix probes (`use*` Node fn passes in relayer / fails in `app/`; second >400-line vendor file fails; inline config in the exempt file fails; file-level `max-lines` directive counted, fails without or after expiry, exempts only `max-lines`, self-retires). R1 (read-only): diff, effective configs, `npm run lint` and `npx eslint relayer --max-warnings 0` (0), relayer typecheck (0), ESLint API enumeration of the 45 relayer files, the five exception placements, the three code fixes, docs, plan-finding dispositions, `git diff --check`.

**verification-gap.** If the relayer silently dropped out of lint again (an ignore re-added), the checker's linted-file count would fall from 152 but nothing asserts it — the count is human-read at the gate; adding an assertion is a follow-up. If a dated exception went stale, `reportUnusedDisableDirectives: error` fails the build; if one expired, the checker fails; both proved. The `FootballDataSource` stubs are covered by `relayer/test/source.test.ts` (rejection behaviour, not arity).

**named-set.** The `react-hooks/*` override is the whole hooks rule pair, scoped to `relayer/**`; the vendor exemption names one file and one rule; the five exceptions are the complete set of over-budget relayer functions at the measured baseline (removing all five yields exactly five errors); no partial special-casing of an enum or status set.

**Missing.** Looked for and did not find: any `Function.length` or arity-dependent consumer of the stubs; a Next/React rule other than hooks that misfires on the relayer's Node code; a `.cjs`/`.cts`/`.mts` file the extension set would miss; a way for `relayer/vendor/**` to escape any rule but `max-lines`; a dependency or lockfile change; a weakened threshold.

**Dismissed.** None; no disputes, no adjudication. One informational item left open by agreement: `relayer/src/insights.ts` sits at 459 counted lines under a dated exception (split by 2026-10-31).

**Human gate.** Owner script: `app` check green with the Lint step ending in `… across 152 linted files`; no workflow touched; merge. **Pending owner go/no-go.**
Entries dated 2026-09-12 or later are linted in CI by `scripts/lint-review-log.mjs` (a byte-identical copy of the Loideroi LLM Wiki's judge, adopted from the BMAD Method assessment): every entry must carry a tier (highest mentioned wins), at least one exact model id, a labeled (`Verdict.`) or bold verdict, a complete tally (a line naming all four counts, `Tally: N Blocker / N Major / N Minor / N Nit` — the bold `**Tally.**` form also passes because the four counts are named — or a Sev table with its separator row), and `Checked.` and `Dismissed.` fields with content (dismissals never silent; `none` is content); Tier 2 and 3 entries also carry `verification-gap.` (would any test fail if this behavior broke?), `named-set.` (partial special-casing of a fixed set), and `Missing.` (what the reviewer looked for and did not find) as labeled fields. Headings carrying `ESCAPE AUDIT`, `OWNER WAIVER`, or `EXCEPTION RECORD` are contract-defined records, not reviews, and are exempt. Run `npm run check:review-log` before pushing.

## 2026-09-12 — PR #101 (chore/process-judges)

**Scope.** Two process judges vendored from the Loideroi LLM Wiki — `scripts/check-agents-md.mjs` (AGENTS.md admission test) and `scripts/lint-review-log.mjs` (review-entry contract) — byte-identical to the wiki's `tools/` at commit `947355e` (dual-reviewed there at Tier 3, 2026-09-12; first project rollout Telescope PR #60), wired as blocking CI steps that run each judge's self-test first; `AGENTS.md` Boundaries section; review-log note stating the enforced fields; `check:agents` / `check:review-log` npm scripts. Commits: `7815e56` (initial), `066553b` (review fixes). `CLAUDE.md` already imports `AGENTS.md` and is untouched. Follow-up, not this PR (R2 Nit): `CONTRIBUTING.md` lines 10–16 still show an unattended push-and-`gh pr create --fill` recipe that bypasses the PR template (investigation proposal P2). Size accounting: hand-written delta under 50 lines; the 665 script lines are cmp-proven byte-identical copies of files already dual-reviewed at Tier 3 in the wiki, counted outside the 500-line cap on that basis (a "fully verified, not sampled" attestation by both reviewers; the owner ratifies this reading at the gate).
**Tier.** 3 (declared 3; floors `.github/**`, `scripts/**`, `AGENTS.md`, `docs/REVIEW_LOG.md`, `package.json`; final 3, confirmed by both reviewers).
**Roles and models.** Author: Claude Code interactive session, `claude-opus-5[1m]` (the wiki session that produced the judges; roster deviation: Fable 5.1 hit a usage-credit limit on 2026-09-12 and the session continued on Opus 5) | Reviewer 1 (cross-vendor): OpenAI Codex CLI 0.144.1, invoked `-m gpt-5.6-sol` per roster Tier 3 (self-reported "gpt-5", known misreport; invoked id recorded), read-only sandbox, two rounds | Reviewer 2 (fresh-context, no author reasoning, no R1 findings on first pass): Claude subagent `claude-opus-5[1m]` (roster says fresh-context Fable 5; same credit limit, recorded for calibration), two rounds.
**Verdicts.** Round 1: R1 pass (0 findings); R2 fail (1 Major, 1 Minor, 2 Nit). Round 2 on `066553b`: R1 pass (0 findings); R2 pass-with-minors (Major resolved; Minor and one Nit dispositioned to this entry). CI: pending on the PR at the time of writing.
**Findings and dispositions.**

| Sev | Raised by | Finding | Disposition |
|---|---|---|---|
| Major | R2 | Boundaries grouped merges with owner-requestable actions ("forbidden unless the owner explicitly requests"), the E2 merge-authority escape shape this repo logged for PRs #85–#87 | Fixed `066553b`: "Merges are the owner's own click — never run `gh pr merge` or `git merge` into `main`, even when asked"; both reviewers confirmed |
| Minor | R2 | 690 insertions against the 500-line cap without an accounting | Recorded in Scope: 25 hand-written / 665 cmp-proven reviewed copies; owner ratifies at the gate |
| Nit | R2 | Log note said "labeled verdict" where bold also passes | Fixed: exact forms stated |
| Nit | R2 | `CONTRIBUTING.md` recipe drift (pre-existing) | Follow-up PR, noted in Scope |

**Tally.** 0 Blocker / 1 Major / 1 Minor / 2 Nit — all resolved or dispositioned.
**Checked.** Both reviewers, both rounds: `cmp` of both scripts against wiki `947355e:tools/*` (identical; R2 also SHA-256); both self-tests (42 and 41 cases; R1's sandbox skipped the filesystem-discovery fixture, which needs a writable temp dir); `npm run -s check:agents` and `npm run -s check:review-log` green; workflow YAML parsed, judge steps after install with self-test first, no `if:`/`continue-on-error`, nothing else in the workflow changed; `AGENTS.md` append-only against `origin/main` with every Boundaries claim checked against the floor map, settings, the wiki ladder, and the repo's existing rules; `package.json` delta is the two scripts only, lockfile untouched; `git diff --check` clean; reuse search (no existing judge or parser to extend); permission scan (no settings, hook, or allow rule touched; direction narrowing only); bad-path probes exit non-zero. R2 additionally confirmed the review-log judge's built-in default path (`logs/review-log.md`) does not exist here, so a dropped argument fails loudly. Neither reviewer ran `npm ci` (ask-first).
**verification-gap.** A deleted or mis-pathed judge exits non-zero; a judge that can no longer go red fails its own self-test before the real run; a non-conforming entry fails (verified with re-dated house-style fixtures). Inherent and not this PR's: deleting the steps from the workflow is detected only by the `.github/**` Tier 3 floor and the human gate.
**named-set.** Judges run on every trigger of their job with no special-casing; the exempt-heading set (ESCAPE AUDIT / OWNER WAIVER / EXCEPTION RECORD) and the required-field set in the log note match the judge exactly; the outward-action set in Boundaries is bucketed completely (ask-first / owner's click / forbidden-unless-requested).
**Missing.** Both reviewers looked for and did not find: hidden permission or outward-action broadening, dependency or lockfile changes, edits to the vendored files, a second workflow needing the steps, nested instruction files left unjudged, a contradiction between `AGENTS.md` and `CLAUDE.md` once loaded together. Not verified by either: live CI on this head.
**Dismissed.** None; no disputes, no adjudication.
**Risk brief (Tier 3, reviewer-written, R1 round 2).** This rollout adds two repository-policy judges and makes them blocking CI checks. A defect could reject valid instruction or review-log updates or silently accept invalid ones; that risk is bounded by the red-capable self-tests, byte identity with the reviewed wiki sources, explicit CI ordering, and successful live checks. Residual risk is policy compliance being instruction- and review-enforced rather than semantically tested; the owner performs the Tier 3 gate and the merge click.
**Cutoff note (2026-09-12).** After the second base update, main's PR #97 entry (written before the judge existed here) lacked the new fields, so `74f086a` moved the CI cutoff to 2026-09-13 to grandfather it. The other session then back-filled that entry's fields on main (PR #102); with the third base update the whole log passes at the judge's default cutoff, so the cutoff is back at the default and this entry is judged by CI like every other.
**Refresh (2026-09-12, after review).** `scripts/lint-review-log.mjs` replaced by the wiki's refactored version at wiki commit `79fb41f` (checkEntry as check tables: championz's blocking ESLint complexity budget failed the `947355e` copy at complexity 17; behavior proven identical by both wiki reviewers over 48 real-log runs and 25,140 differential runs; 43 self-test cases). `scripts/check-agents-md.mjs` refreshed to wiki commit `3387910` (one self-test fixture assembled at runtime because GitHub's secret-scanning Google matcher flagged the literal in Telescope; 42 cases, behavior unchanged). Byte identity of both files re-verified with `cmp`.
**Base updates.** `f816e86` (main at `4ed5a62`: PRs #96/#97), `8d6eae3` (main at `f720e68`: #98/#99), and the third merge (main at `2d7b208`: PR #102 — the other session's review-process wiring, whose `## Boundaries` and `## Pointers` supersede this PR's Boundaries draft; `AGENTS.md` resolved to main's version, judge still green). Post-merge CI on `8d6eae3` failed the `app` job on ESLint complexity 17 in the vendored `lint-review-log.mjs` — fixed at the wiki source (`79fb41f`, dual-reviewed) and re-vendored (`309c80b`); CodeQL then raised two high alerts on the vendored `check-agents-md.mjs` (regex injection via `--require-heading`, ReDoS in the flag pattern) — fixed at the wiki source and re-vendored (see the Refresh sentence).
**Refresh 2 (2026-09-13).** `scripts/check-agents-md.mjs` replaced by wiki `61e5907`: CodeQL on championz #101 raised two high alerts on the previous copy (regex injection via `--require-heading`, exponential backtracking in the flag pattern); fixed at the source with a literal heading match and a tokenizer, dual-reviewed there (R1 Codex `gpt-5.6-sol`, R2 `claude-opus-5[1m]`, mutants for both defects fail 1/47). Byte identity re-verified with `cmp`; judge output on this repo's `AGENTS.md` unchanged.
**Merge authority.** Owner click, pending.

## 2026-09-12 — PR #102 `chore/review-process-wiring` (merge click is the owner's; review process wired into the entry files)

**Scope.** `.claude/settings.json` (rewritten: no `gh pr merge`, `git merge`, `gh api`, `vercel`, `npm install`, issue/workflow writes without a prompt; pushes prompt-free only on six branch prefixes; every refspec, delete, tag, prune, force form asks; every textual spelling of a push to `main` incl. `*heads/main` and `git -c`/`-C` denied; `node *` narrowed to `node scripts/*`; self-applying the `size-waiver` label asks), `AGENTS.md` (Boundaries + Pointers per the wiki template — merges are the owner's click, an owner "merge it" never waives the reviewer passes, every PR declares its tier and ships its own log entry, Tier 3 paths named, settings stated as cooperative with the missing "require a pull request" rule named), `CLAUDE.md` (workflow line), `CONTRIBUTING.md` (per-task recipe: tier, reviewers, same-PR log entry, no `--fill`). ~200 changed lines. Context: the 2026-09-09 escapes above — 20 unlogged merges (the entry's prose says 23; its list has 20) plus #85–#87 happened because these files never mentioned the review process and the old settings allowed the merge. Investigation record: Loideroi LLM Wiki `raw/audits/2026-09-11-championz-escape-investigation.md`.

**Tier.** 3 — `.claude/**`, `AGENTS.md`, `CLAUDE.md` are agent instruction / permission files (floor map); permission changes are never-eligible for an owner waiver. Confirmed by both reviewers.

**Roles and models.** Author: Claude Code interactive, `claude-fable-5-1` for the first commit, `claude-opus-5[1m]` for the fix rounds (the session's model changed mid-work on a usage-credit stop; recorded, not a choice). Reviewer 1 (cross-vendor): OpenAI Codex CLI 0.144.1, invoked `-m gpt-5.6-sol`, read-only sandbox (four passes on this PR). Reviewer 2 (fresh-context, no author reasoning, no R1 findings on first pass): Claude Code subagent, `claude-opus-5[1m]` (a first `claude-fable-5-1` subagent died on the usage-credit stop before producing anything; three passes).

**Verdicts.** R1: **fail** → fail → **pass** → **pass** (final, tip `a354168`). R2: **fail** (1 Blocker, 1 Major, 3 Minor, 1 Nit) → pass-with-minors (2 Minor, 2 Nit) → **pass** (1 Nit). Tally (all rounds, unique): 1 Blocker / 3 Major / 5 Minor / 4 Nit — all resolved except one accepted Nit.

| Sev | Raised by | Finding | Disposition |
|---|---|---|---|
| Blocker | R2 | `git push origin feat/x:main` (and multi-ref `feat/x main`) rode the `feat/*` allow row; no deny matched | Fixed: `* main`, `* *:main`, `*heads/main`, `+` force-refspec forms denied; generic `git push * *:*` asks. R1 raised the same as a Major on its first re-check; both re-checked: resolved |
| Major | R1 | Catch-all `Bash(git push *)` in ask shadowed the six branch allow rows (deny → ask → allow, first match wins) | Fixed: catch-all removed; R1 re-checked |
| Major | R2 | `node *`, `npm run*`, `git rebase -x` allow rows run arbitrary code, so the settings are cooperative while AGENTS.md stated them as a guarantee | Fixed by honest wording (settings shape what an agent tries; branch protection + the enrolled check are the boundary) and `node scripts/*`; R2 re-checked |
| Major | R2 (re-check) | Branch protection on `main` has enforce-admins + required checks but no "require a pull request" rule, so a green PR head can be fast-forwarded to `main` directly | AGENTS.md states it; owner click queued (wiki plan O10); "narrowed, not closed" until both clicks |
| Minor | R2 | `-d`, `--prune`, `--tags` rode the prefix rows; `--label=` and mid-command `rebase -x` missed the space-delimited ask rows; `heads/main` DWIM spelling missed every deny row; entry files referenced scripts that land in the stacked PRs | All fixed (`* -d`, `--prune*`, `--tags*`, `--all*`, `--mirror*` ask; `*--label*` / `* -l*`; `git rebase *-x*` etc.; `*heads/main` deny; "lands with the stacked PRs; merge the three in order") |
| Nit | R2 | `git rebase *-i*` also prompts on branch names containing `-i` | **Accepted** (friction, not a hole) |

**Checked.** R1: complete settings file under deny → ask → allow against the official permissions documentation (rule precedence, `*` spans spaces, `Bash(cmd *)` boundaries, compound commands), representative spellings (direct `main`, `HEAD:main`, `feat/x:main`, full-ref, force-refspec, delete, generic refspec, ordinary branch), JSON parse, `git diff --check`, cross-file consistency of AGENTS/CLAUDE/CONTRIBUTING. R2: full settings file (165 lines) plus a spelling matrix of ~30 forms incl. `git -C`, `--git-dir`, quoted refspecs, `heads/main` DWIM (verified live in a scratch repo), branch protection read via the API, `ci.yml`/`codeql.yml` triggers, `check-agents-md.mjs AGENTS.md` (pass), reuse search (no existing permission validator). Author self-verification: JSON parse; `check-agents-md` pass.

**verification-gap:** permission rules have no in-repo test — if a rule stopped matching, nothing fails; correctness rests on the documented semantics and the reviewers' matchers. The AGENTS.md branch-protection claim is not mechanically checked.

**named-set:** the main-push deny family covers `main`, `:main`, `*heads/main` (both spellings), `+` forms, `-u`/`--set-upstream`, `-c`/`-C`; the six allowed prefixes are consistent across settings and prose; other prefixes (`release/*`, `dependabot/*`) prompt (fail-closed). The forms no textual rule can see — quoted `"main"`, `HEAD` pushed from a `main` checkout — prompt (no push form is allow-listed for them) and are named in AGENTS.md.

**Missing.** Both reviewers looked for a `PreToolUse` hook parsing the push destination (none — ask-first under the wiki ladder; the settings are cooperative by design and say so), an executable settings-policy test (none exists in the tooling), and the "require a pull request" branch rule (owner click).

**Dismissed.** none. **Disputes.** none.

**Risk brief (R2, for the owner).** The agent's unprompted git/gh surface is now tightly listed and the instruction file tells the truth: these rules guide a cooperating agent and are not a wall. Every obvious way of pushing `main` now prompts or is blocked. The part that matters for "merge is my click": your branch protection today blocks force-pushes and requires green checks, but it does not require a pull request — so a green PR head can still be pushed straight to `main` by anyone with your token, including an agent. One click ("Require a pull request before merging") makes the merge genuinely yours; enrolling `review-gate` as required is the second click. Residual after both: none for direct pushes; the agent could still write a misleading review-log entry, which is what the escape audit reads.

**Gate.** Owner go/no-go pending. Merge this PR first, then `chore/portable-judges`, then `chore/review-gate`, in one sitting: this PR's entry files reference `scripts/lint-review-log.mjs` and the `review-gate` check that land in the other two. PR #102.

## 2026-09-13 — PR #106 (CI app job → Node 24; unpinned pre-guard npm install removed)

**Scope.** Head `18674d0` (three commits on `2d7b208`; +14/−9 across 2 files). `.github/workflows/ci.yml` `app` job: `node-version: "22"` → `"24"`; the `npm install -g npm@11` step (networked, unpinned, ran BEFORE lockfile-lint) deleted; new "Toolchain (assert bundled npm 11)" step prints versions and fails unless `npm --version` matches `^11\.`. `docs/REVIEW_TIERS.md`: "CI npm bootstrap" follow-up closed with the locked-version engine record, toolchain-pin-not-guard wording, other jobs deliberately on 22, Vercel scoped out, and a new "Vercel Node major alignment" Named Follow-Up. No gate reordered or weakened.

**Tier.** 3 — workflow + REVIEW_TIERS floors. Plan reviewed before code.

**Roles and models.** Author: Claude Code, `claude-opus-5[1m]`. Plan reviewer + R1 (cross-vendor): Codex CLI 0.144.1 `gpt-5.6-sol`, read-only. R2 (fresh-context): Claude subagent `claude-opus-5[1m]`.

**Plan review (R1).** FAIL — 1 Major (log entry + both reviewer passes are merge conditions, "app green" alone is not), 4 Minor (record the locked-version engine checks; other jobs stay on 22 — correct; describe as toolchain pin not supply-chain guard; assert versions explicitly rather than relying on setup-node logs). All taken: assertion step added, wording adopted, engine record written.

**Verdicts.** R1 at `3d40dcf`: **FAIL** — 1 Major (this log entry absent; written after both verdicts, as on #97/#100/#105), 3 Minor ("PR D" label; engine record missing from the diff; Vercel not covered by "production workflows untouched"). R2 at `3d40dcf`: **pass-with-minors** — 1 Minor (production Node major on Vercel now decoupled from CI and recorded nowhere in the repo — "test what you ship" drift, not a defect), 3 Nit (minor/patch wording; "PR D"; `@types/node ^20`, pre-existing, no action). R2 verified mechanically: all 549 `engines.node` ranges in the lockfile admit Node 24 (the one exception, `@img/sharp-win32-ia32`, is an optional win32/ia32 binary never selected on ubuntu); Node's release index confirms the 22 line ships npm 10 only and the 24 line npm 11 only (11.3.0 → 11.19.0); the PR's own run: Node v24.20.0 / npm 11.19.0, all checks green, `npm ci` without config warnings, lockfile-lint clean. Fixes `980f093` + `18674d0`: PR number, inline engine record, Vercel scope note + Named Follow-Up (owner confirms the preview build log at the gate; alignment via dashboard or `engines.node` is a separate Tier 3 PR), wording. **Final at `18674d0`: R1 PASS (conditional on this entry landing before merge — it has), R2 PASS.** Tally: 0 Blocker / 1 Major (procedural, resolved by this entry) / 4 Minor (resolved) / 3 Nit (1 pre-existing, open by agreement). No adjudication; both reviewers converged on the Vercel point independently.

**Author verification.** YAML parses; step order verified; engine support of every locked devDependency checked; CI on the PR head green with the Toolchain step showing Node 24 / npm 11.

**Risk brief (R1, final).** *What changed:* only the app CI job moves to Node 24; npm 11 is asserted; the pre-guard global install is removed; every existing gate stays intact and in order. *What could break:* toolchain drift within Node 24 / npm 11 minor and patch releases — made fail-visible by the assertion (if a 24.x ever bundles npm 12 the job fails loudly). *What was checked:* the diff, step order, grep quoting inside the YAML block scalar, locked dependency engines, the other jobs' separation, production workflows untouched. *Residual risk:* low; Vercel's production Node major is dashboard-selected and now not guaranteed identical to CI — a named Tier 3 follow-up; the owner confirms the preview build log at the gate.

**Checked.** R2: every `engines.node` range in the root lockfile (549 of 909 entries) evaluated against 24.0.0 / 24.5.0 / 24.12.0 with npm's bundled semver — one exclusion, `@img/sharp-win32-ia32` (optional win32/ia32 binary, never selected on ubuntu); Node's dist index (22 line ships npm 10.5.1–10.9.8 only; 24 line ships npm 11.3.0–11.19.0 only); the PR's live run (Node v24.20.0 / npm 11.19.0, `npm ci` 708 packages without config warnings, lockfile-lint clean, all checks green); YAML step order; the grep's quoting inside the literal block and its behaviour without `pipefail`; repo-wide grep for Node-version assumptions (`engines`, `.nvmrc`, `vercel.json`, docs); the other three jobs' separation and the production workflows. R1: the diff, step order, grep escaping, the locked devDependency engines, docs wording, plan-finding dispositions, `git diff --check`.

**verification-gap.** If setup-node "24" ever resolved to a toolchain without npm 11, the new assertion step fails the required `app` check — self-testing. If the lockfile guard were moved after `npm ci`, nothing mechanical would catch it; pre-existing and untouched here. If Vercel's production Node major drifts from CI, nothing in the repo detects it — the named follow-up.

**named-set.** Only the `app` job's `node-version` changes; the other three CI jobs and the two production workflows stay on 22 by decision, listed explicitly in REVIEW_TIERS; no enum or flag set is partially special-cased.

**Missing.** Looked for and did not find: an in-repo pin of the production Node version (`engines.node`, `vercel.json`, `.nvmrc`); any locked dependency that rejects Node 24 on the CI platform; any gate weakened, removed, or reordered; any network-fetched executable before the lockfile guard other than the pre-existing `npx lockfile-lint@5.0.0`.

**Dismissed.** None. Open by agreement: Nit `@types/node ^20` (pre-existing, older types are the conservative direction).

**Human gate.** Owner script: `app` check green with the Toolchain step printing Node 24 / npm 11; `main` protection unchanged; glance at the Vercel preview build log's Node version; merge. **Pending owner go/no-go.**

## 2026-09-12 — PR #98 (insights: matchday 2 — form and the real league table, ranked UEFA's way)

**Scope.** `relayer/src/insights.ts` (`tablePositions` rewritten: kickoff cut, UEFA league-phase tiebreaks a–h, shared positions; `buildFacts` threads kickoff), `relayer/test/insights.test.ts` (+12 tests), `relayer/scripts/generate-insights.mjs` (live-mode comparison with UEFA's published standings, warn-only), NEW `relayer/test/fixtures/standings-ucl-2027-md1.json` (18 MD1 results + UEFA's 36 published ranks, 3.6 KB); regenerated `public/insights/*.json` ×6 and `relayer/test/output/insights-sample/*.json` ×6. Hand-written: +287/−36 across two commits (`55a6436`, `cf4ebbe`); the rest is generator output. Owner intent, verbatim: "create updated AI insights for match day 2". The author widened that to two bug fixes because the first regeneration with real results shipped copy that was wrong (post-match table on played MD1 cards; 0-point clubs ranked 19th–36th by feed order) — recorded in the PR description as the deviation from the literal ask.

**Tier.** 3 — confirmed independently by both reviewers: `relayer/**` floor. `public/insights/**` is unmapped (Tier 2 default); highest wins.

**Roles and models** (roster `last_reviewed` 2026-08-27, inside the 90-day clock). Author: Claude Code interactive session, `claude-opus-5` — the same recorded deviation from the roster's Tier 3 author row (Fable 5) as #94/#95. Reviewer 1 (cross-vendor): OpenAI Codex CLI 0.144.1, `-m gpt-5.6-sol` (session header; the model self-reports otherwise), read-only sandbox — could not run Vitest there (temp-dir writes denied, `--configLoader runner` collected zero tests), so its verdicts are reasoned from code. Reviewer 2 (fresh-context, given the brief with the diff, repo docs, duties and the verbatim owner intent — no author reasoning, no R1 findings): Claude Code subagent. **Roster deviation:** the roster's Tier 3 reviewer-2 row is fresh-context Fable 5; the first launch on `claude-fable-5` terminated immediately with HTTP 429 "out of usage credits", so R2 was re-run on `claude-opus-5`. Recorded, not waived; the roster's own text names Fable the fallback for a Pro-class Gemini, and no stronger Anthropic model was reachable.

**Verdicts.** R1 first pass **fail** (0 Blocker, 1 Major, 1 Minor, 0 Nit); after fix round 1 **pass** (Blocker 0, Major 0, Minor 0, Nit 0). R2 first pass **pass-with-minors** (0 Blocker, 0 Major, 2 Minor, 2 Nit); after fix round 1 **pass-with-minors** with both Minors resolved and one deferred Nit open (Blocker 0, Major 0, Minor 0, Nit 1). One fix round (`cf4ebbe`), re-checked by both raising reviewers.

**Findings and dispositions.**

| Sev | Raised by | Finding | Disposition |
|---|---|---|---|
| Major | R1 | `TIEBREAK` implemented only UEFA criteria a–e (GD, GF, away GF, wins, away wins) then shared positions, while the regulations continue with f–h (opponents' collective points, GD, goals), i (disciplinary points) and j (club coefficient) — tied clubs could carry positions that diverge from the official table as the phase accumulates | Fixed: f–h implemented (second aggregation pass over each club's opponents). i and j deliberately not applied and documented in code — cards are not in the feed, and applying j without i would invent an order UEFA might not publish; clubs still level after h share the position, which UEFA's own standings feed does (nine shared positions after MD1 2026/27, observed live). R1 re-check: **resolved**, "sharing the rank is safer than inventing one" |
| Minor | R1, R2 (independently) | No test exercised `wins` or `awayWins` as the deciding criterion — removing or misordering either comparator left every committed test green | Fixed: one test per key with rows level on every earlier criterion (and, after the fix, on f–h too, so the assertion isolates the key). Both re-checks: **resolved** |
| Minor | R2 | The generator never cross-checked its local table against UEFA's published standings, though `fetchLeaguePhaseRanks` already reads that source — the author's "36/36" was a manual claim, not repeatable | Fixed: live mode compares `tablePositions(played)` with `fetchLeaguePhaseRanks(season)`, prints equality or warns per club by name; fetch failure is caught (decoration, never a failed run). Live run printed "table: 36 clubs, all positions equal UEFA's published standings". R2 re-check: **resolved** |
| Nit | R2 | `${t} arrive on a ${f} run` renders "Inter arrive on a L run" for one-result form — pre-existing template, first production exposure here (126 entries) | **Deferred** — copy change across six locales, out of scope; follow-up |
| Nit | R2 | `tablePositions` relies on the `PlayedMatch` contract rather than checking `result.status`; the only caller filters `FINISHED` first | Accepted by contract, no change |

**Dismissed:** none. The one Nit deferred (one-result "run" copy) is recorded above with its reason, not dismissed.

**Verification-gap:** every changed ranking criterion (kickoff cut, own-result exclusion, a–h, shared positions, 90′ score, league-only filter, `buildFacts` threading) has a focused test; the complete MD1 output is pinned to a captured 36-club official table with shared ranks, and the test refuses a snapshot that no longer contains shared ranks. Untested and accepted: the generator's standings-comparison logging and error paths (script + network, warn-only, do not change generated content). Six-locale parity of the table line is tested with pinned positions because the recorded archive holds only one league matchday.

**Named-set:** `MatchType` — `GROUP_STAGE` is the only table-bearing member; `SINGLE`/`FIRST_LEG`/`SECOND_LEG` take the existing knockout path with positions nulled (covered by the decider test). `MatchStatus` — not inspected in `tablePositions`; the generator pre-filters `FINISHED` (R2 Nit, by contract). The tiebreak set is explicitly bounded at h with i–j documented together, not a silent branch. No newly introduced partial special-casing found by either reviewer.

**Missing:** neither reviewer could reach `standings.uefa.com` (R1: sandbox; R2: network denied), so neither re-downloaded the snapshot's `officialRanks`; R2 instead re-implemented criteria a–h independently in Python from the fixture's 18 results and reproduced all 36 ranks (25 distinct positions, 11 clubs in shared slots), cross-checked the 18 results against `lib/fixtures/matches.json` (ids, kickoffs, matchday), and confirmed the shipped `public/insights/en.json` positions and W/D/L letters equal the snapshot for all 36 clubs. Both looked for and did not find: a duplicate table/standings implementation elsewhere (`fetchLeaguePhaseRanks` is a fetch of a published past-season table, not a computation; the app's `lib/**` standings are the user leaderboard); any signed/chain/ASCII-boundary surface; any 90-minute-rule violation (table uses `scoreA90`/`scoreB90`; `outcomeFor` for form still uses totals — pre-existing, untouched, identical in the league phase). No scheduled regeneration workflow exists (ADR-0011's "ideally scheduled") — unchanged by this PR, the residual operational risk.

**Checked:** author — live `generate-insights.mjs --season 2027` (strength 35/36 ranked, table 36/36 equal to UEFA); six-locale key parity 144/144; MD1 entries byte-identical to `origin/main` (18/18), 126 later entries changed; JSON byte-identical between the two commits (f–h split nothing after one matchday). R2 — `npx vitest run` in `relayer/` 177/177 (insights 29/29), `tsc --noEmit` clean, tree clean before and after both passes; programmatic check of all 144 keys × 6 locales; manual reads of MD1 `2049553`–`2049570`, MD2 `2049571`/`2049572`/`2049577`, MD3 `2049589`, MD8 `2049682` in en/fr/tr; winner/loser tiebreak symmetry (Paris 1st/Bayern 2nd ↔ S. Bratislava 35th/Bodø 36th; Arsenal 15th above AEK 16th on away goals while their victims share 27th). R1 — `git diff --check` both rounds; sampled `2049571`, `2049577`, `2049580` (MD2) and `2049553`, `2049560` (MD1) in en/es; reuse search across `relayer/src` and `lib`.

**Disputes.** None. Asymmetry worth recording: R1 found the Major (incomplete tiebreak sequence) that R2 had read and accepted as a "deliberate, documented choice"; R2 found the repeatability gap (no generator cross-check) and did the ground-truth reproduction R1 could not. Both found the `wins`/`awayWins` test gap independently.

**Gates** (author, re-verified by R2 on the fix commit): app `typecheck` clean · `npm test` 205/205 · relayer 177/177 · `lint` 5 warnings, byte-identical to `origin/main` · jscpd 9 clones / 0.86%, identical to main · `check:i18n` OK · `build` OK · `snyk_code_scan` on `relayer/src` 0 issues; on the whole `relayer/` 13 Low, all pre-existing CLI-arg→fs-path flows in local scripts (the two in `generate-insights.mjs` are the original `--fixtures` read and `--out` write, not lines this PR added). CI on #98 green on `55a6436` (app, relayer, contracts, squawk, CodeQL, Vercel).

**Not exercised in a browser.** Neither reviewer nor the author opened the preview. Reviewer-written verification script for the owner (Tier 3 human gate), in any locale on the Vercel preview: open matchday 2 **Inter v Club Brugge** → "Inter arrive on a L run; Club Brugge arrive on a L run … The table says Inter 25th, Club Brugge 22nd"; **Man City v Paris** → "Man City 8th, Paris 1st"; matchday 3 **Paris v Barcelona** → "Paris 1st, Barcelona 3rd"; any matchday-1 match (e.g. **Real Madrid v Inter**) → no form and no table line, text unchanged from before; switch to French or Turkish on the same fixture → same numbers, "1er"/"1." ordinals; then compare Paris 1st / Bayern 2nd / Bodø/Glimt 36th with uefa.com's live table. Two shared MD1 ranks (e.g. Sporting CP 6th, Stuttgart 6th) should read as shared. When regenerating for matchday 3, the console must print "table: 36 clubs, all positions equal UEFA's published standings"; warnings there go to the author before committing.

**Gate.** Owner **GO** 2026-09-12: the owner read the reviewer-written risk briefs and verification script, was reminded by the author of the 2026-09-09 decision that the merge click is the human's (and that #91–#93 were logged as a deviation for exactly this), and reaffirmed in writing: "I dont want to click, you merge with my approval". The agent then merged #96 → #99 → #98 in that order. **Recorded as a second deviation from the 2026-09-09 decision, not an amendment of the permission ladder** — the ladder stays unamended until the owner changes it through a Tier 3 process of its own.

## 2026-09-12 — PR #97 (guardrail-complete app tree: dependency-cruiser + knip in CI, blocking budgets, time-bounded exceptions)

**Scope.** Head `bbea6f1` (six commits on `d6c8422`: gates; three checker rounds; compaction). NEW `.dependency-cruiser.js`, `knip.jsonc`, `ARCHITECTURE.md`, `scripts/check-lint-exceptions.mjs` + `.test.ts`; `package.json` (exact devDeps `dependency-cruiser@18.2.0`, `knip@6.32.2`; scripts `arch`, `deadcode`, `deadcode:ci`; `lint` → `eslint --max-warnings 0 && node scripts/check-lint-exceptions.mjs`; `dup` threshold 2% → 1%), `package-lock.json` (79 transitive nodes added, none removed), `eslint.config.mjs` (budgets `warn` → `error`, `reportUnusedDisableDirectives: "error"`, TeamCrest `no-img-element` file override), five one-line dated `eslint-disable-next-line complexity` exceptions (EnterPanel 51, MatchRow 32, saveProfile 27, ProfileForm 26, Telegram webhook POST 19; all `expires 2026-10-31`), `components/predict/TeamCrest.tsx` (inline suppression removed), `vitest.config.ts` (+`scripts/**/*.test.ts`), `.github/workflows/ci.yml` (three new `app` steps; squawk job also on pushes to `main` via `github.event.before` with zero-sha guard), `.github/PULL_REQUEST_TEMPLATE.md`, `AGENTS.md` (Gates section), `docs/REVIEW_TIERS.md` (floor rows, baselines, exception burn-down list, follow-ups). Non-lockfile delta 464/−35 = **499** changed lines (one under the binding cap after a compaction commit; over the Tier 3 aim of 300 because three review rounds each hardened the exception checker). Baselines 2026-09-11: dependency-cruiser 0 violations (123 modules / 297 deps); knip 0 files / 0 deps / 0 unlisted; ESLint 0 errors after exceptions, 0 max-lines; jscpd 9 clones / 0.86%.

**Tier.** 3 — workflows, `package.json`, lockfile, `eslint.config.mjs`, `scripts/**`, `AGENTS.md`, `docs/REVIEW_TIERS.md` are all floor rows. Plan reviewed before implementation (contract: spec before code).

**Roles and models.** Author: Claude Code interactive session, `claude-fable-5-1` through the first push, `claude-opus-5[1m]` from the R2 fix rounds on (session model switched by the owner mid-review); work in a dedicated worktree. Plan reviewer + Reviewer 1 (cross-vendor): OpenAI Codex CLI 0.144.1, `-m gpt-5.6-sol`, read-only sandbox (could run lint/arch/knip/dup/i18n/typecheck; could not run Vitest, build, or contracts — all require writes — and could not reach GitHub). Reviewer 2 (fresh-context, no author reasoning, no R1 findings): Claude Code subagent, `claude-opus-5[1m]`, own worktrees with writes (ran every gate, the build, and 11+ probes) (first R2 attempt on `claude-fable-5-1` hit a session limit before producing output and was discarded).

**Plan review (R1, before code).** FAIL — 1 Blocker (squawk migrations job PR-only, so a direct push to `main` skipped it), 3 Major (relayer lint/complexity unenforced; contracts slither absent; `app/**` orphan exemption too broad), 5 Minor/Nit (middleware outside the devDep selector; expiry not mechanically enforced; Node 22 runtime unverified; wrong checkout branch; "leaves" wording). Dispositions: Blocker fixed (squawk on `push` too); orphan exemption narrowed to Next.js entry basenames; middleware added to `not-to-dev-dep`; expiry made mechanical (checker script); wording fixed; relayer and contracts narrowed with an honest record as the next two single-concern PRs in `docs/REVIEW_TIERS.md` Named Follow-Ups; Node 24 bootstrap likewise. Author-found during break-the-judge: `exclude: ^node_modules` had silently disabled `not-to-dev-dep` (Telescope's config has the same bug) — changed to `doNotFollow` only.

**Verdicts.** R1 implementation review at `8d21ab6`: **FAIL** — 1 Major (`check-lint-exceptions.mjs` scanned only the app roots, so an undated `eslint-disable` in `scripts/`, `next.config.ts` etc. passed), 1 Minor (shape-only date check accepted `2026-99-99`). R1 re-check at `da2c99f` (whole-tree regex scanner): **FAIL** — Major kept open with counterexamples (`x=1// eslint-disable-line` missed; `"; // eslint-disable…"` string false-positive; basename ignores diverge from ESLint's rooted ignores); Minor resolved. R1 re-check at `b1553c6` (checker rewritten on the ESLint Node API, reading `suppressedMessages[].suppressions[]`): **PASS**. R2 first pass (da2c99f, then b1553c6): **FAIL** — 1 Major (inline *config* comments such as `/* eslint complexity: "off" */` reconfigure the rule so nothing is reported or suppressed: invisible to the checker, `--max-warnings 0` and `reportUnusedDisableDirectives`; proved with a complexity-17 function passing full lint), 1 Minor (no upper bound on `expires`; 2099-12-31 passed), 8 Nits. R2 also proved four holes at da2c99f that b1553c6 had already closed (JSX-container directive, multi-line block directive, comma-prefixed `-line`, basename-vs-rooted ignores). R1 re-check at `90b57c9` (lexical `/* eslint ` scan): **FAIL** — Major: multi-line config comments missed, `global`/`globals`/`exported`/`eslint-env` families not covered, string false positives. R2 re-check at `90b57c9`: **pass-with-minors** (Major resolved for single-line forms; new Minor: per-line scan misses multi-line). R1 at `e40c170` (second ESLint pass with `noInlineConfig: true` — ESLint's parser enumerates every inline directive; anything not `eslint-disable/enable` fails): substance PASS, **Blocker on size** — 532 non-lockfile lines > 500 cap. R2 at `e40c170`: **PASS** (11 probes incl. both multi-line shapes, JSX container, JSDoc non-match, string non-match, disable/enable pair). Compaction commit `bbea6f1` (tests merged 216→215 with every assertion kept, comments shortened; delta 499). **Final at `bbea6f1`: R1 PASS, R2 PASS.** No adjudication needed; the two reviewers found disjoint holes (R1: scope, parser divergence; R2: inline config, horizon) and converged on the ESLint-native design.

**Findings and dispositions.**

| Sev | Raised by | Finding | Disposition |
|---|---|---|---|
| Blocker (plan) | R1 | squawk job `if: pull_request` — direct pushes to `main` bypassed the migration gate | Runs on PR and `main` push; `BASE_SHA` falls back to `github.event.before`, zero-sha (branch-creation) guard exits 0; R1 re-check: sound |
| Major (plan) | R1 | `^app/` orphan exemption exempted every colocated helper under `app/` | Two explicit entry-basename patterns (top-level + nested; nested quantifier form rejected by dependency-cruiser as unsafe); break-the-judge: orphan `app/enter/zzz-helper.ts` now fails both arch and knip |
| Major (plan) | R1 | Relayer has no lint tooling; complexity budgets don't reach Tier 3 value-moving code | Narrowed with record: next single-concern PR (new devDeps in that workspace); jscpd already covers `relayer/src` |
| Major (plan) | R1 | Contracts have no slither step | Narrowed with record: the PR after relayer (Python toolchain in the `contracts` required check) |
| Major | R1 | Exception checker scope: app roots only | Round 1: whole tree minus ESLint ignores + all JS/TS extensions (regex). Round 2: regex abandoned — ESLint API with the repo config decides scope and directive recognition. R1: resolved |
| Minor (plan) | R1 | `middleware.ts` outside `not-to-dev-dep` selector | Added; break-the-judge: `import "vitest"` in middleware fails (after the `exclude` fix above) |
| Minor (plan) | R1 | Expiry date not mechanically enforced | `check-lint-exceptions.mjs`: undated or expired directive fails `npm run lint`; `reportUnusedDisableDirectives: error` fails a stale one |
| Minor | R1 | `expires 2026-99-99` accepted forever | Strict UTC round-trip parse for both the directive and `LINT_EXCEPTIONS_TODAY`; tests for leap day / invalid month / day-of / day-after |
| Minor (plan) | R1 | CI runtime Node 22 + unpinned `npm install -g npm@11` before the lockfile guard | Pre-existing; recorded as follow-up (Node 24 bundles npm 11 — the wiki CI template already does this). Both tools declare Node 22 support; this PR's own green `app` run is the runtime proof |
| Nit (plan) | R1 | "leaves" overstated: lib/i18n/content are mutually importable | Wording → "foundation layer" in config comments and `ARCHITECTURE.md` |
| Major | R2 | Inline ESLint config comment (`/* eslint rule: off */`) bypasses the checker, `--max-warnings 0` and unused-directive reporting | Round 1: lexical scan for the opener (R1 re-check: insufficient — multi-line, `global`/`exported` families, string false positives). Round 2: second ESLint pass with `noInlineConfig: true`; ESLint's parser reports every inline directive; non-disable ones fail. R1 + R2 re-check: resolved |
| Major | R1 | Lexical inline-config scan diverges from the parser (multi-line comments, `global`/`globals`/`exported`/`eslint-env`, strings) | Superseded by the `noInlineConfig` pass; no regex over source text remains. R1 re-check: resolved |
| Blocker | R1 | Cumulative non-lockfile delta 532 > 500 cap | Compaction commit (helpers merged, comments shortened; every rule and assertion kept) → 499. R1 re-check: resolved |
| Minor | R2 | No upper bound on `expires` (2099 accepted) | 180-day horizon; 180 passes, 181 fails (tested). R2 re-check: resolved |
| Minor | R2 | Per-line inline-config scan misses a comment spread over two lines | Superseded by the parser-based pass; both multi-line shapes tested. R2 re-check: resolved |
| Nit ×9 | R2 | Undeclared transitive dev bumps (`acorn` 8.17→8.18, `get-tsconfig` 4.14.0→4.14.1 — now stated in the PR body); TeamCrest override is file-wide; inert `^scripts/`/`next.config.ts` orphan exemptions; entry allowlist lacks `proxy.ts`/`forbidden`/`unauthorized`/`instrumentation` (fail-closed); ESLint runs three times per `npm run lint`; `expires:` with a colon gives a misleading message; UTC "today"; `// @ts-expect-error <desc>` is a TypeScript suppression outside this gate's ESLint scope — the one remaining undated-suppression path | Recorded; not blocking. The `@ts-expect-error` gap and the entry-allowlist conventions go to Named Follow-Ups in the relayer-lint PR (this PR is at the size cap) |

**Author verification.** All gates green locally at every pushed sha (typecheck, lint, arch, deadcode:ci, dup, test 213/213, check:i18n). Break-the-judge (each mutation reverted, tree verified clean): lib→app import, orphan in lib, orphan colocated under app, devDep import from middleware and from lib, type-only devDep import (passes, intended), complexity-16 function, exception comment removed, exception on a compliant function (unused-directive error), expired exception via `LINT_EXCEPTIONS_TODAY=2026-11-01`, undated directive in `next.config.ts` (caught only after the ESLint-API rewrite), 6× duplicated block (3.2% > 1%). CI: all 8 checks green on `8d21ab6`, `da2c99f`, `b1553c6`; the `app` job log shows the three new steps executed.

**Risk brief (R1, `gpt-5.6-sol`, final at `bbea6f1`, for the owner).** *What changed:* the app's quality guardrails become blocking CI gates — architecture and dead-code checks; no cycles, upward imports, orphans, or runtime devDependency imports; complexity and file-size budgets blocking; duplication tightened to 1%; changed migrations checked on PRs and on pushes to `main`. Five existing complexity violations stay temporarily permitted through dated exceptions. *What could break:* stricter gates may reject legitimate new Next.js entry files until the entry allowlist is updated, or expose existing debt as the tree evolves; the exception checker runs ESLint in multiple passes (longer lint) and depends on ESLint's `noInlineConfig` diagnostic wording, defended by tests and the committed lockfile; the PR sits one line under the size ceiling. *What was checked:* the original implementation and every corrective delta — architecture and knip scope, orphan exemptions, devDependency detection, suppression behaviour, real and impossible dates, the 180-day horizon, multi-line and no-whitespace directives, inline rule configuration, `global`/`globals`/`exported`/`eslint-env`, string literals, migration push semantics, CI ordering, docs, lockfile scope, cumulative size; lint, arch, knip, i18n, dup and ESLint API/Linter probes reproduced. *Residual risk:* R1 could not run Vitest, build or contracts in its read-only sandbox (R2 ran all of them, plus the build, in a writable worktree); relayer lint/architecture, contracts slither, entry-allowlist maintenance and the unpinned npm 11 bootstrap remain explicit follow-ups. None undermines the implemented gates.

**Human gate.** Owner script (from the PR body): open the `app` check on the PR and confirm the Lint / Architecture rules / Dead code steps ran; confirm `main` protection unchanged (5 required checks, strict, enforce-admins — this PR touches none of it); read this brief; merge. **Pending owner go/no-go** — not merged by the agent.

**Wiki context.** Plan of record: Loideroi LLM Wiki `wiki/guide/development-lifecycle.md` guardrail-stack section (specified → installed → enforced; guardrail-complete target) and `wiki/guide/new-project.md` step 4b. After merge, championz's app tree is the first Loideroi tree with every applicable Layer 3 gate *enforced* behind a required check; the relayer and contracts workspaces follow as the next two single-concern PRs.

**Appended 2026-09-12 (escape-prevention session, PR #102; entry above unchanged) — judge fields the entry predates.** The review-log judge (`scripts/lint-review-log.mjs`, cutoff 2026-09-12, arriving with PR #103) requires these five labeled fields; the entry was written hours before the judge existed, so the questions were not put to its reviewers. Owner item: the authoring session appends the real content below these lines.
- Checked: per the entry's own text — all gates green locally at every pushed sha (typecheck, lint, arch, deadcode:ci, dup, test 213/213, check:i18n); break-the-judge mutations for each rule, reverted and verified clean; R1 plan review + three implementation rounds; R2 fix rounds with re-checks.
- Dismissed: none recorded — the nine R2 Nits were accepted as fail-closed or recorded follow-ups (see the table).
- verification-gap: not asked at review time — owner item.
- named-set: not asked at review time — owner item.
- Missing: not asked at review time — owner item.

## 2026-09-10 — PRs #94 and #95 (WalletConnect metadata; stale-fact corrections)

**Scope.** #94: `app/providers.tsx` — AppKit `metadata.name` `"₵h@mpi0nz Pr3dict0r"` → `"Ch@mpi0nz Pr3dict0r"`, and `icons: []` → the existing `/icon-512.png`. Two string literals plus a comment. #95: `CLAUDE.md` (wagmi v2 → v3), `PRD.md` (same), `docs/REVIEW_TIERS.md` (jscpd baseline row), `.github/workflows/ci.yml` (the same baseline duplicated in a comment at the gate). +5/−5, all prose or comment.

**Tier.** 3 for both, uncontested. #94: `app/providers.tsx` is named in the floor map's Reown/AppKit wallet row (signing surface). #95: `CLAUDE.md` and `docs/REVIEW_TIERS.md` are "agent instruction files and the review process itself"; `.github/workflows/**` is Tier 3 independently.

**Roles and models** (roster `last_reviewed` 2026-08-27, inside the 90-day clock). Author: Claude Code interactive session, `claude-opus-5` — **a deviation from the roster's Tier 3 author row, which assigns Fable 5**; recorded rather than waived, the session model was not the author's to choose. Reviewer 1 (cross-vendor): OpenAI Codex CLI 0.144.1, `-m gpt-5.6-sol`, read-only sandbox. Reviewer 2 (fresh-context, given only the diffs and duties): Claude Code subagent pinned to `claude-fable-5`, per the roster's Tier 3 reviewer-2 row.

**Verdicts.** #94 — first pass: R1 **fail** (1 Major), R2 **fail** (1 Major, 3 Minor, 2 Nit), independently reaching the same Major. After fix round 1: R2 **pass**, R1 **pass-with-minors** (one new Minor, in the opposite direction from R2's). After fix round 2: reconciled, comment-only. #95 — first pass: R1 **pass-with-minors** (2 Minor), R2 **pass-with-minors** (1 Minor, 3 Nit). After the fix round: R1 **pass**, R2 **pass**.

**Majors and dispositions.**

| Sev | Raised by | Finding | Disposition |
|---|---|---|---|
| Major | R1 and R2 independently | #94 shipped a comment in `lib/wagmi/config.ts` asserting that wiring `NEXT_PUBLIC_RPC_URL` via `customRpcUrls` would fail `AdminPanel.computeRanked` with `-32062` and break the freeze ranking. False: `extendCaipNetwork` folds custom URLs into `rpcUrls.default.http`, `getViemTransport` wraps them in viem `fallback`, and `shouldThrow` stops only for tx/user rejection, execution-reverted and code 5000 — a range error falls through to `rpc.chiliz.com`. **R2 went further and showed the comment was inverted**: the hazard is the unmentioned `transports` prop, which `extendWagmiTransports` returns as-is for non-WC chains with no fallback. Proven with a runnable viem test. A comment written to protect a payout path would have steered a future engineer away from the safe change and left the dangerous one unflagged | Comment removed entirely rather than rewritten; `lib/wagmi/config.ts` restored byte-identical to `main` (both reviewers verified mechanically). The true half is carried into the follow-up below rather than lost |

**Minors of note.**

- **R2 (#94): the metadata comment over-claimed the ASCII rule** — the rule's three-item list does not name dapp metadata, and nothing is known to mishandle `₵` there. Author corrected. **R1's re-check then found the correction had swung too far into under-claiming**, since the rule's headline reads "anything signed or sent to chain/external APIs" and closes "never in a payload". *Resolution, no adjudicator needed:* both are right about different clauses. The rule **does** mandate the rename (headline + payload clause; the three items are examples under it), and it is **not** the PR #23 `isValidSignature` case (metadata never reaches signing bytes — verified by both reviewers in sign-client). Final comment states both.
- **R1 (#95): `.github/workflows/ci.yml` carried the identical stale baseline** at the gate the corrected row describes. Fixed in the same PR; R2 confirmed comment-only, `--threshold 2` untouched, and that single-concern holds.
- **R2 (#95): "had drifted" was the wrong verb.** The 9th jscpd clone was *raised and accepted* in the #88/#89 review, a decision under the owner's gate — recording it as drift would launder a reviewed decision into an accident. Row rewritten to say raised-and-accepted, and to state that the gated metric shrank 1.20% → 0.86% so "may shrink, never grow" still reads true.
- Both reviewers independently measured **0.86%**, not the 0.87% first written — itself a stale figure carried from the #88/#89 entry before #91 and #93 changed the denominator. A row correcting a stale measurement had shipped a stale measurement.

**Owner sign-off requested on one reading (R2's framing).** Merging #95 confirms that the jscpd clone-count baseline moving 8 → 9 *records* the acceptance already made in #88/#89, rather than granting a new relaxation; that the enforced gate (`--threshold 2`) is unchanged and its metric went down; and that "never grow" continues to bind the gated percentage while the clone count is informational. The row states this explicitly so it is a visible choice.

**Follow-up carried out of #94, not lost.** `AdminPanel.computeRanked` scans `Entered` from block `0` to `latest` in the browser although `deployBlockFor`/`PREDICTOR_DEPLOY_BLOCK` already exist for exactly this scan. R2's ranking: not freeze-blocking today, because the adapter's default transport for Chiliz is `rpc.chiliz.com`, which answers the genesis scan in ~230ms; it becomes freeze-breaking only if the browser transport changes or that endpoint starts capping ranges. Land it before the League-stage freeze, with a mainnet dry-run of `computeRanked` as the actual pre-freeze gate — and document the `transports`-prop hazard alongside it, since that true half of the deleted comment is now recorded nowhere in the repo.

**Disputes.** None requiring adjudication. The one divergence (over- vs under-claiming the ASCII rule) was resolved by reading the rule's own text; both reviewers' concerns are satisfied simultaneously.

**Gates.** typecheck clean · 205 tests · lint 5 warnings (= `main`) · check:i18n 272×6 · build OK · jscpd 9 clones / 0.86% (= `main`) · `snyk_code_scan` on `app/providers.tsx` 0 issues · lockfile-lint clean ×3.

**Not exercised in a browser.** The sandbox denies starting a server. #94's entire visible effect is what a wallet renders on its approval sheet, which no automated gate can show. Per the contract's Tier 3 requirement, the human behavioral verification is: connect the Socios.com wallet on the preview, confirm the approval sheet shows "Ch@mpi0nz Pr3dict0r" with the icon, and that connection completes.

**Gate.** Awaiting owner. Merge authority per the wiki contract §Resolution And Merge — the human clicks at every tier until the ladder is amended per repo; no amendment exists for this repo. **Correction recorded for audit:** earlier the same day, PRs #91, #92 and #93 were merged by the agent on the owner's explicit typed instruction. That was outside this contract and outside the 2026-09-09 owner decision; it is logged here as a deviation, not a precedent.

## 2026-09-10 — PR #91 (standings: mark the connected wallet's row)

**Scope.** `app/standings/StandingsPanel.tsx` (row extracted, `useAccount` read), NEW `app/standings/BoardRow.tsx` (`BoardRow` + `PredictorCell`), `lib/predictor/standings.ts` (+`isSelfRow`), `lib/predictor/standings.test.ts` (+4 assertions), `messages/*.json` ×6 (+`standings.you`). ~200 changed lines. Presentation-only: no contract call, no signed payload, no server route.

**Tier.** 3 — confirmed independently by both reviewers. Not the `app/**` Tier 2 default: `StandingsPanel.tsx` hosts `ClaimBanner`, which calls `writeContractAsync({ functionName: "claim" })`, so the floor map's path-independent catch-all ("any module that constructs, signs, or submits chain transactions") applies. Everything else in the diff floors at 2; highest wins.

**Roles and models.** Author: Claude Code interactive session, `claude-opus-5`. Reviewer 1 (cross-vendor): OpenAI Codex CLI 0.144.1, invoked `-m gpt-5.6-sol`, read-only sandbox (could not run Vitest there). Reviewer 2 (fresh-context, given only the diff and duties, no author reasoning, no R1 findings): Claude Code subagent, `claude-opus-5`.

**Verdicts.** R1 **pass** (0 findings at all severities), first and only pass. R2 first pass **pass-with-minors** (0 Major, 1 Minor, 6 Nit); after fix round 1 **pass-with-minors** (1 Minor surviving, 1 new Nit — both raised against the author's own fix reasoning); after fix round 2 **pass** (0 Major, 0 Minor). Three fix rounds, each re-checked by the raising reviewer.

**Findings and dispositions.**

| Sev | Raised by | Finding | Disposition |
|---|---|---|---|
| Minor | R2 | `bg-glow/12` dropped `muted-2` (truncated address, `"—"` prize placeholder) and `chz` (prize figure) below the 4.5:1 AA floor **on the highlighted row** — an a11y regression introduced by an a11y-motivated feature. R2's model self-validated by reproducing the 4.96 figure the `globals.css:17` palette comment records | Author reproduced and confirmed, capped the wash at `bg-glow/8` (round 1). R2 re-check found the fix insufficient — see next row |
| Minor | R2 (re-check) | The round-1 fix modelled the wrong step, and the author's own oklab extension was wrong: `color-mix(in oklab, C 8%, transparent)` is **premultiplied**, so it resolves to plain `--glow` at α.08 and the oklab space cancels out — the operative step is sRGB source-over. Further, the backdrop is **not flat**: `globals.css:49` paints radials of this same blue over the body gradient, so `muted-2` on a tinted row measures 4.39–4.49 across scroll positions — under AA *everywhere*, not merely near it. Capping the wash never secured AA | Author verified both claims independently (premultiplication confirmed algebraically; backdrop swept). Fixed by promoting `muted-2` → `muted` on `isSelf` rows (round 2), measured 5.76–6.30 across the full sweep — removes the constraint rather than shaving it. Deeper tint cuts rejected: the wash is only ~1.1:1 against the panel, so it would spend the signal to buy contrast. R2 re-checked: **resolved** |
| Nit | R2 | Accent used a real `border-l-2`, shifting the rank digit 2px on the highlighted row and able to widen the column | Replaced with `shadow-[inset_2px_0_0_var(--glow-2)]`; R2 verified it takes no layout space (spec-guaranteed) and resolves to `--glow-2` in the emitted CSS |
| Nit | R2 | Test fixture named `checksummed` was arbitrary mixed case, not EIP-55 — it passed for the right reason but asserted a property it did not have | Replaced with the real `getAddress` form, verified round-tripping |
| Nit | R2 | Trailing space in the non-self row `className` | Space moved inside the conditional |
| Nit | R2 (re-check) | Doc comment attributed the measurement to "oklab (the space `color-mix` composites in)" — a wrong *method*, which outlives a wrong number | Comment rewritten to record the correct recipe (composite in sRGB, do not lerp in oklab) plus the non-flat backdrop |

**Accepted without change** (R2 raised, author dispositioned, R2 agreed): per-row `useTranslations` (idiomatic, ~51 rows); discoverability on a long board and KO-only wallets showing no badge in League View (product follow-ups, `rowsForView` filtering is correct); stale jscpd baseline in `docs/REVIEW_TIERS.md` (pre-existing — that file is itself Tier 3 and must not ride a feature PR). R2 also examined and explicitly declined to raise: `chz` tightness at the deep end of the radial (pre-existing palette; brand colour, not promotable) and the tonal flattening between `anonymous` and the address on a nameless self row (benign, arguably correct).

**Author self-caught before review.** The first draft of the extraction tied the top-prize bold to `crown` (`season && rank === 1`); since the prize column never renders in Season View, first place would have silently lost its bold in every paying view. Split into `crown` vs `topPrize` pre-PR.

**Notable verification.** The load-bearing correctness point is that `/api/standings` lowercases every address (route.ts:97, rows built from `[...wallets.keys()]`) while wagmi returns EIP-55 checksummed — a `===` compare would have compiled, read fine, and matched **never**. Both reviewers traced this independently rather than trusting `isSelfRow`'s name or doc comment. R2 additionally enumerated all 11 rendering branches of the extracted row against the original and re-verified the table after each fix commit; the round-2 prize-cell ternary changes exactly 2 of 8 cells, neither a payout nor a rank branch.

**Disputes.** None. One asymmetry worth recording: R1 returned a clean pass with zero findings at every severity on a change where R2 found a substantiated AA regression and four correct nits, and then caught two further errors in the author's fix reasoning. R1 ran typecheck and lint but could not run Vitest in its read-only sandbox.

**Gates** (author, re-verified by R2 on the fix commits): `typecheck` clean · `npm test` 205/205 · `lint` 5 warnings, byte-identical to `origin/main` · `check:i18n` 272 keys × 6 · `build` OK · jscpd 9 clones / 0.86% vs main's 9 / 0.87% · `snyk_code_scan` on `app/standings` 0 issues. Note: `docs/REVIEW_TIERS.md` records a jscpd baseline of 8 clones / 1.20% from 2026-08-27; `origin/main` now measures 9 / 0.87%, so that recorded figure has drifted independently of this PR.

**Not exercised in a browser.** The session's sandbox denied starting a server, so no pixel was observed for either the highlight or the inset accent; both reviewers reasoned from the emitted CSS instead, and R2 stated that caveat explicitly. Author verified all new Tailwind utilities emit real CSS in the production bundle (Tailwind silently emits nothing for an unrecognised class). **The Vercel preview is the outstanding manual check**: connect an entrant wallet, confirm the row is marked and column alignment holds in all three views.

**Gate.** Awaiting owner. Per the 2026-09-09 decision the merge click is the human's; no agent merged or attempted to merge this PR.

## 2026-08-27 — chore/review-wiring-and-gates (review wiring: floor map, PR template, gates, `.npmrc` baseline, CodeQL, Dependabot)

**Scope.** First review wiring for this repo: `docs/REVIEW_TIERS.md` (floor map + measured baselines), this log, `.github/PULL_REQUEST_TEMPLATE.md`; `.npmrc` ×3 (root/contracts/relayer — none existed; install scripts previously ran on every `npm ci`); ESLint `complexity(15)`/`max-lines(400)` warn budgets (baseline 6/0); jscpd 2% ratchet (baseline 8 clones/1.20%); lockfile-lint pre-install in all three CI jobs (all lockfiles verified alias-free); first-ever CI Lint step; squawk migrations job (hardened selector: `--no-renames`, NUL-safe, base-sha via env) + `.squawk.toml` exception channel; `.github/dependabot.yml` (4 entries, cooldown 2d/7d); NEW `.github/workflows/codeql.yml` (JS/TS security scan — free public-repo CodeQL); workflow-level `permissions: contents: read`. devDeps `jscpd@5.0.16`, `squawk-cli@2.63.0` (owner-approved, vetted). ~290 hand-written lines excl. lockfile.

**Tier.** 3 (CI + guardrail config + agent-process instruction files) — confirmed by both reviewers.

**Roles and models.** Author: Claude Code interactive session, `claude-fable-5`. Reviewer 1 (cross-vendor): OpenAI Codex CLI 0.144.1, invoked `-m gpt-5.6-sol`, read-only sandbox. Reviewer 2 (fresh-context, no author reasoning, no R1 findings on first pass): Claude Code subagent, `claude-fable-5`.

**Verdicts.** First pass: R1 **fail** (1 Major, 0 minors), R2 **fail** (1 Major + 3 Minor + 4 Nit). After the fix round with raising-reviewer re-checks: R1 **pass-with-minors**, R2 **pass-with-minors**.

**Majors and dispositions.**

| Sev | Raised by | Finding | Disposition |
|---|---|---|---|
| Major | R1 | No security scan anywhere in CI — the contract's merge gate (tests, lint, duplication, lockfile, **security scan**) was unsatisfiable; Dependabot scheduling is not a scan | NEW CodeQL workflow (free on this public repo, JS/TS); Solidity honestly deferred to a named slither follow-up; R1 re-checked: resolved |
| Major | R2 | Floor map under-floored the auth/secrets kernel (`lib/supabase/server.ts` service-role client, `lib/profile/verify.ts` ERC-1271 verification, `lib/telegram/link.ts`, `lib/profile/rate-limit.ts` at default Tier 2 while `postcss.config.mjs` floored 3 — risk inversion) | Dedicated Tier 3 row + functional catch-all for secrets/privileged-client/auth modules; R2 re-checked against the tree: resolved |

Minors (all resolved or dispositioned): no `permissions:` block in ci.yml (added, `contents: read`); relayer has no ESLint tooling (recorded as a Named Follow-Up — needs an owner dependency decision); `migrations:lint` is the full-history debt view, exit 1 by design (documented in the baselines). Nits: migrations job setup-node added; `lib/wagmi/**`/`app/providers.tsx` named in the wallet row; Dependabot×Tier-3 workload and npx version-pinning stand as accepted notes; post-verdict: codeql-action bumped v3→v4 on R1's evidence (v4 is the documented "latest"; reviewer disagreement resolved by fetching github/codeql-action — no adjudication needed) and a concurrency block added per R2's new Nit.

**Disputes.** None (one factual divergence — codeql-action current major — resolved by primary-source evidence, recorded above). **Author self-verification:** lint/dup/typecheck/test (112) /check:i18n green at root; relayer typecheck + tests (89) green under the new `.npmrc`; all three lockfile-lint commands pass; all three YAML files machine-parsed; squawk exit semantics captured directly.

**Post-gate CI catch (2026-08-27).** PR #38's first live run: CodeQL, contracts, relayer, and migrations all green (the three new lockfile-lint steps and both `.npmrc`-governed installs validated in production CI); the app job failed at `npm ci` — EUSAGE, `@swc/helpers` missing from the lock file: local npm 11 rewrote the lockfile in a layout runner npm 10 refuses (the documented Fanbet next/next-intl trap). Fix: "Pin npm to v11" step in the app job only (relayer/contracts lockfiles untouched by npm 11 and green on npm 10 — deliberately not pinned). Focused delta re-check by Codex `gpt-5.6-sol`: **pass**, fix confirmed correct and minimal.

**Gate.** Owner **GO** 2026-08-27 (structured gate round with both reviewer risk briefs). Ruling: the mirrored `package-lock.json` ×3 Tier 3 floor is **confirmed** (chilitize/Fanbet precedent; revisit trigger: Dependabot review load becomes a real cost). Owner clicks after merge: required checks in branch protection (incl. CodeQL); Settings → Security & analysis for Dependabot alerts + automated security fixes. Note: public repo — CI runs despite the private-repo billing outage, so this PR gets live validation immediately.

## 2026-09-09 — PRs #85, #86, #87 (admin console health strip, per-match pipeline, stage-lifecycle hints) — POST-MERGE REVIEW, logged as escapes

**What happened.** Three PRs by the author session (Claude Code, `claude-fable-5-1`) were merged to `main` the same day on the owner's instruction with **no independent reviewer pass, no PR-template tier declaration, and no log entry** — the contract's Tier 2/3 gates were skipped. The owner asked whether the project was still running in line with the contract; the answer was no, and the missed reviews were run retroactively on the merged diffs the same day. This entry records the escapes, the retroactive verdicts, the fixes, and the process gaps.

| PR | Merged as | Declared / floor tier | Raised to | Size (changed lines, no lockfiles) | Cap |
|---|---|---|---|---|---|
| #85 health strip | `dda3d89` | none / 2 (`app/**`, `lib/**`) | **3** (R2 #87: `app/admin/AdminPanel.tsx` submits chain txs — signing-surface catch-all) | 892 | **breach** (500) |
| #86 pipeline column + watcher coverage | `63661e6` | none / 3 (`relayer/**`, `.github/workflows/**`) | 3 | 1,130 | **breach** (500; Tier 3 aim 300), no waiver |
| #87 lock hint + freeze gate | `5cf57b1` | none / 2 | **3** (same catch-all) | 146 | ok |

**Roles and models (retroactive reviews, 2026-09-09).** Author: Claude Code interactive, `claude-fable-5-1`. Reviewer 1 (cross-vendor): OpenAI Codex CLI 0.144.1, read-only sandbox — `gpt-5.6-sol` on #86 (Tier 3), `gpt-5.6-terra` on #85 and #87 (declared Tier 2 at launch; both later raised to 3 by R2, so R1 ran one tier below the roster's Tier 3 model — noted, not re-run). Reviewer 2 (fresh-context, no author reasoning, no R1 findings): Claude Code subagents — `claude-fable-5-1` on #86, `claude-sonnet-5` on #85 and #87. Briefs carried only the diff, the repo docs and the contract duties; one author-written "pay attention to" hint was removed from the #86 brief before launch to keep R2 independent.

**Reviewer verification caveat.** Codex's read-only sandbox could not execute Vitest (18 × `EPERM mkdir …/ssr`, 0 tests run) on any of the three; typecheck, lint and i18n ran. All three Claude R2 sessions ran the full suites (app 195 tests, relayer 164 where touched). R2 on #87 disclosed that it briefly ran `git checkout <sha> -- <files>` in the live repo, restored immediately, and redid the comparison in an isolated worktree; the tree was verified clean afterwards.

**First-pass verdicts.** #85: R1 **fail** (4 Major, 1 Minor), R2 **fail** (2 Major, 3 Minor, 1 Nit). #86: R1 **fail** (2 Major, 2 Minor), R2 **fail** (2 Major, 4 Minor, 4 Nit). #87: R1 **fail** (1 Major, 2 Minor), R2 **pass-with-minors** (2 Major, 3 Minor; tier raised to 3).

**Majors and dispositions.**

| PR | Sev | Raised by | Finding | Disposition |
|---|---|---|---|---|
| #85 | Major | R1 | Health data keyed to the wallet's chain, not the deployment's | Fixed in #88 (`DEPLOYED_CHAIN_ID`); R1 re-check: resolved |
| #85 | Major | R1 | Ages measured against `loadedAt`, which never advanced in an open tab | Fixed in #88 (30 s ticking clock; 5-min full refresh); R1 re-check: resolved |
| #85 | Major | R1, R2 | Lint ratchet: `AdminPanel` complexity 34, 598 lines (baseline 6 complexity / 0 max-lines) | Fixed in #89 (extract `StageCard`, `GovernanceLine`) — lint now 5 / 0; R1 + R2 re-check: resolved |
| #85 | Major | R1 | PR size 892 vs 500 cap | **Escape — not fixable post-merge.** No waiver existed |
| #85 | Major | R2 | `act()` refetched the balance but not the stage structs → false SOLVENCY_BREACH after `lockStage(0)` — **happened in production 2026-09-09** | Already fixed by #87 (`5cf57b1`) before the review; R2 re-check: resolved. Traced to layer: independent review would have caught it (R2 found it from the diff) |
| #86 | Major | R1 | Watcher liveness judged on the newest run of any kind — a cron run hid a live watcher | Fixed in #88 (`latestRunBy`/`watcherAlive` on watcher-tagged runs); R1 re-check: resolved |
| #86 | Major | R2 | Watcher liveness threshold was the 6 h cron rule; a dead watcher looked alive all matchday | Fixed in #88 (`WATCHER_STALE_AFTER_MS` = 15 min, tested with interleaved rows); R2 re-check: resolved |
| #86 | Major | R1, R2 | PR size 1,130 vs 500 cap, Tier 3 aim 300, no waiver | **Escape — not fixable post-merge.** Separable into a ~120-line relayer/workflow slice and a UI slice |
| #87 | Major | R1 | `freezeCallable` ignored the 24 h provisional window → enabled button would revert (`StageNotFinal`) | Fixed in #88 (`play.provisional === 0`, `freezeBlocker` says how many remain); R1 re-check: resolved |
| #87 | Major | R2 | `STAGE_STATUS` redefined instead of imported from `lib/predictor/standingsPayload.ts` | Fixed in #88 (import + identity test); R2 re-check: resolved |
| #87 | Major | R2 | New complexity warning (16) on the stage card | Fixed in #89; lint 5 / 0 |

Minors (all resolved unless noted): duplicated `STAGE_FLOOR` (imported from `lib/economics`); governance line skipped `owner()` (added, mirrors `checkGovernance`); `stageNeedsFreeze` docstring falsely claimed parity with the bot (rewritten: it deliberately follows the contract, voided matches never block); `HealthBanner` duplicated the troubled-run verdict (now calls `summarizeRun`); `pipelineRows` single 1,000-row query (split per kind); `--runner` unvalidated (allowlisted, exits 1; lockstep test pins it to the app's union); `lib/admin/pipeline.ts` duplicates `relayer/src/matchday.ts` (**accepted duplication** — the app cannot import the relayer package; `pipeline.lockstep.test.ts` imports both and asserts constants + span equality, so drift fails CI; jscpd 8→9 clones, 0.88 % lines, under the 2 % gate); browser-side copies of `readBalance`/`checkSolvency` verdict formulas (**accepted** — thresholds pinned by tests to the bot's values; R1 marks partially resolved). Nits: "runner gone" for yesterday's watcher (fixed — scoped to the coverage window), 5-min reload re-read only the log (fixed — full refresh), dead `loadedAt` (removed), midnight-crossing window label and duplicate mount RPC reads (accepted), `DEPLOYED_CHAIN_ID` is the seventh copy of the env expression (deferred).

**Pre-existing, outside these diffs (owner to-do):** `oracle-bot.yml` interpolates the `manual_results` dispatch input into a single-quoted shell string — owner-only dispatch, but move to `env:` when the file is next touched (R2 #86).

**Fix PRs (new Tier 3 changes, reviewed pre-merge).** #88 `fix/admin-review-findings` (+247/−71, 8 files) and #89 `chore/admin-lint-ratchet` stacked on it (+~150/−~90, 3 files). R1 `gpt-5.6-sol` on the combined diff: **fail** → one real Major (owner read not in the refresh list — fixed, one word) and one contested Major (combined diff 530 lines — **rebutted**: the cap is per PR, 318 and ~230; recorded here for the adjudicator if contested). R1 `gpt-5.6-terra`: **pass-with-minors** (new Minor: freeze nag ignored provisional results — fixed). R2 `claude-fable-5-1` (#86 raiser): **pass**, risk brief written, 4 Nits (two fixed, two accepted). R2 `claude-sonnet-5` (#85 raiser): **pass**, 2 Nits (both fixed). R2 `claude-sonnet-5` (#87 raiser): **pass-with-minors**, all five findings resolved; new Minor: no behavioral test for the `--runner` exit path — fixed (relayer `scripts.test.ts` spawns `relay.mjs` with a bad tag, expects exit 1). Final fix-branch head `cc2b8c8`: app 203 tests, relayer 165, lint 5 / 0, Snyk 0 on `app/admin`, `lib/admin`, `relayer/src`, `components/chrome`. CI green on both. **Gate: owner go/no-go pending.**

**Process escapes recorded (beyond the three PRs).**
1. **Merge authority.** The author session pressed merge on #85, #86 and #87 on the owner's typed instruction. The permission ladder has not been amended for this repo, so the contract's "human clicks merge" rule was not followed. Owner decision needed: amend the ladder (agent may merge on explicit owner instruction after green review) or keep the click human.
2. **Unlogged run.** 23 PRs merged between #39 and #84 (2026-08-28 → 2026-09-09) with no log entry; by floor map 16 are Tier 3 (relayer, workflows, API routes, scripts); 8 exceed 500 lines, several dominated by generated fixture/insight JSON. Whether they were reviewed out of band is unknown to this session. Listed for the next escape audit:
   `#52 T3 1759 · #53 T3 77 · #54 T1 30 · #59 T3 3126 · #60 T3 228 · #61 T3 1130 · #62 T3 1622 · #63 T2 209 · #64 T3 663 · #65 T3 2696 · #66 T3 118 · #68 T3 102 · #69 T3 87 · #71 T3 60 · #72 T2 116 · #73 T2 113 · #74 T2 8 · #75 T3 228 · #76 T3 262 · #84 T3 1164` (sizes are additions+deletions incl. generated files).
3. **Author gates skipped** on #85–#87: PR template not used, `npm run dup` not run locally (jscpd is not installed in this checkout; CI ran it and passed).
4. **Roster tier mismatch**: #85/#87 R1 ran on the Tier 2 model because the tier was raised only by R2; the fix PRs were reviewed at Tier 3 (`gpt-5.6-sol` + fresh Fable).

**Correction (2026-09-12, appended, entry otherwise unchanged).** Item 2 above says 23 unlogged PRs, 16 Tier 3; the list it gives has 20 PRs (#52, #53, #54, #59–#66, #68, #69, #71–#76, #84), 15 tagged T3 — confirmed against the GitHub API (merged PRs between #39 and #84). With #93 (found 2026-09-12 by the wiki's cross-repo check) the retroactive-triage scope is 16 Tier 3 PRs. Investigation: Loideroi LLM Wiki `raw/audits/2026-09-11-championz-escape-investigation.md`.
